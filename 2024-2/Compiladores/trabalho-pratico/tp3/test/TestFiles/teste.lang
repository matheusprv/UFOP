divmod(num :: Int, div :: Int) : Int, Int {
    q = num / div;
    r = num % div;
    return q, r;
}

main(args :: Char []){
    -- comentario teste
    a = 10;
    b = .10;
    c = 'a';
    d = true;
    e = false;

    iterate (a < 10 && d){
        if (a == 10){
            b = .11;
        }
        else {
            a = (a + 5 - 1) * (6 / (20 % 10));
            a = 5 + 5;
        }

        if (b != 1){
            print (2+1);
            read e;
        }
    }
}